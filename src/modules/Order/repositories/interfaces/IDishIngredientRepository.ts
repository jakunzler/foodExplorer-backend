// import { DishIngredient } from "@prisma/client";

export interface IDishIngredientRepository {
  // getByDishId(dishId: string): Promise<DishIngredient[]>;
  // delete(id: string): Promise<DishIngredient>;
  // deleteAll(dishId: string): void;
}
