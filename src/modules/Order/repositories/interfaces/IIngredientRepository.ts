// import { Ingredient } from "@prisma/client";
// import { CreateIngredientDTO } from "../../useCases/CreateIngredient/CreateIngredientDTO";

export interface IIngredientRepository {
  // create(Ingredient: CreateIngredientDTO): Promise<Ingredient>;
  // getById(IngredientId: string): Promise<Ingredient | null>;
  // getAllIngredients(): Promise<Ingredient[] | null>;
  // update(
  //   IngredientId: string,
  //   updatedIngredientData: Partial<Ingredient>,
  // ): Promise<Ingredient>;
  // delete(IngredientId: string): Promise<Ingredient>;
}
